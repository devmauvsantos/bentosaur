# r003 Attempt 02 — Narrow Window + Normal Transfer

Result: **rejected; visual gate failed; frozen; no further automated tweak**.

The single localized correction changed:

- outer window from `1.18 × 1.20` to `1.10 × 1.12`;
- outer transition placement to sit closer to the body;
- custom-normal transfer from the cut body to the transition;
- a wider outside-window audit exclusion.

The result is worse: the transition boundary becomes a striped ring in front
and three-quarter views. The static cavity and tongue remain useful evidence,
but the seam is not production quality.

The exact recipe used for this attempt is preserved under this attempt's own
`recipes/` directory. The identical root r003 recipe is retained as the
revision's reproducible entry point. Attempt 01's earlier recipe is also
preserved under its own `recipes/` directory.
