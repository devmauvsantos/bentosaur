# Facial Import Contracts

Each versioned directory stores the exact Godot headless contract result for
the matching character import. The unversioned
`../facial_rig_contract_report.json` is the most recent local run.

A passing contract proves that the expected meshes, blend shapes, bones,
triangle budget, and Mobile renderer are present and callable. It does not
replace a human visual gate.
